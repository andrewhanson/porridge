{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @app/web test",
    "test:coverage": "pnpm --filter @app/web test:coverage",
    "packages": "dotenv -- pnpm --filter",
    "web": "pnpm packages @app/web",
    "db:migrate": "pnpm  packages @app/database db:migrate",
    "db:deploy": "pnpm packages @app/database db:deploy",
    "db:studio": "pnpm packages @app/database db:studio",
    "db:generate": "pnpm packages @app/database db:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "dotenv-cli": "^8.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
